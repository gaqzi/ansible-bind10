---
bind10_system_dependencies:
  - build-essential
  - libtool
  - automake
  - lcov
  - pkg-config
  - python3-dev
  - libsqlite3-dev
  - libbotan1.8-dev
  - liblog4cplus-dev
  - libgtest-dev
  - libboost1.48-dev

bind10_system_dependencies_dhcp:
  - libmysql++-dev

# I don't know if these are all really used for just man pages, but
# cargo culting on this one.
bind10_man_dependencies:
  - xsltproc
  - docbook
  - docbook-xsl
  - elinks

git_package_name: git